# Live Views — Build Assumptions & Autonomous Decisions (review log)

> Decisions I make **autonomously** while finishing the spec → Claude-Design prompts → Next.js build (per the 2026-06-19 directive: *"if you have any question you need an answer to while doing it then go with your most recommended answer and save them in a document that we can review later whose impact should be easily translatable into the design and therefore the frontend"*). Each entry: **the question · the call I made · why · the impact lever** (the exact place to change in design/code if you'd decide differently). Mirrors `../ASSUMPTIONS-LOG.md`. **Status: build in progress.**

| ID | Question | Call made (recommended) | Why | Impact lever (where to change) |
|---|---|---|---|---|
| **A-LV1** | Where does the frontend live? | Build the live views **into `sentinx-web`** (the existing Next.js/TS app) under a new route group `/(live)/runs/[id]/...`, reusing its Tailwind/token setup. | One codebase + one theme system across the report + live views (DESIGN.md §, maintainability). Avoids a second app to wire. | `sentinx-web/app/` route group; if you want a standalone app, lift the `(live)` group out. |
| **A-LV2** | Data source, given the engine port (`D-LV-dep3`) isn't built? | Define a **stable `RunView` TypeScript contract** (the projection V2/V3 consume) and feed it from **(a) real captured runs** (`aarav-live/captures/*/state.json`) + **(b) the simulator** + **(c) a thin `/api/runview/[id]` route** that adapts today's `state.json` shape → `RunView`. The engine port later implements the same contract server-side. | Lets the full UI be built + tested now against real data without waiting on the backend port; the contract is the seam. | `sentinx-web/lib/runview.ts` (the contract + adapter); swap the adapter for the real engine endpoint when `D-LV-dep3` lands. |
| **A-LV3** | Dial level numbers (attacks/turns/breadth)? | Use the `D-LV25` proposed mapping as **tunable defaults** (low 8/8/1 … ultra full/30/all; `med`=16/16/1). | The shape is decided; exact values are tunable and surfaced. | `sentinx-web/lib/intensity.ts` (the level→budget map). |
| **A-LV4** | Which screens to build now? | **V2 Arena (full LTO), V3 Forensic (full expansion + Re-judge/Judge-diff), the Run-Intensity dial on Run Config, and the minimal run-flow shell** (run home + the C4-vs-V1 stays per `OPEN-LV1`). **V1 deferred** (`D-LV23`). | Matches the in-scope decisions; V1 is explicitly later. | task list; add V1 when greenlit. |
| **A-LV5** | Real run spans only Compliance today (no Security plays). | Build both **Security + Compliance bands**; the Security band renders **empty/honest** on today's captured runs and **populates** once a both-pillar run is executed (`D-LV-dep4`). I will run a fresh both-pillar attack cycle for the proof. | Honest now, correct when the data arrives; the band structure is `D-LV24`. | the run executed in Task #13 (objective pool + dial). |
| **A-LV6** | The dual-duty paired-twin **connector** UI (one conversation → two linked ribbons across the bands, `D-LV24`). | Types carry `incidentId`/`pairedIdx`, but **no cross-band connector renders yet** — it activates when the engine emits the `D8` two-row split (the connector has nothing to link until then). Logged so it isn't lost. | Honest "render-on-D8"; the most novel game-style element is **deferred to D8**, not dropped. | `arena.tsx` BandView + `lib/runview.ts` `pairedIdx`; wire the connector when `D8`/`D-LV-dep3` emits paired observations. |
| **A-LV7** | The Arena header **sample switcher** — a `<select>` (`?data=…`) that swaps which saved board the page loads. **NOT a product filter**; a build/test aid that exists only because the Arena is fixture-fed while the engine port is parked (`A-LV2`/`D-LV26`). **Decision: keep VISIBLE for the review phase** (user, 2026-06-20); how to scope it is deferred → `OPEN-LV3`. | Keep the switcher rendering as-is during review so scenes can be flipped; do not gate/remove yet. | Reviewer needs to flip between scenes; production scoping is a separate, later call. | `app/runs/[id]/arena/page.tsx` `SAMPLES` + the `<select>`. To dev-gate later: render only behind `?dev=1` / `NODE_ENV!=='production'`. To remove: delete the `<select>`; reach a scene via `?data=…`. **The catalog of entries (what each is + why) ↓** |

### A-LV7 · the sample-board catalog (what each `?data=` entry is, for the later scoping decision)
All live in `sentinx-web/public/runs/<name>.json`. **3 are real captured Aarav runs; 4 are hand-made teaching fixtures.** None are filters.
| `?data=` | Real / fixture | What the board shows | Why it exists |
|---|---|---|---|
| `both-pillar-live` | **real** | Security 4/4 **HELD** + 2 Compliance **breaches** (disclosure/vulnerability bypasses) + 5 errored "not assessed" (tunnel dropped) | the primary proof board |
| `both-pillar-full` | **real** | full Compliance band 8/8 (2 breach, 6 held); 2 Security errored at start | between this + `both-pillar-live`, both bands seen fully populated |
| `recon-demo` | **fixture** (real plays + **hand-filled recon**) | the recon prelude/V3 populated (contact, 3 signals, intel→attack, probe transcript) | deterministic recon-view check (real runs kept blocking recon) |
| `live-8play` | **real** | 8-play run with **degraded judges** (e.g. 2-of-3 voted) | honesty: UI shows "2 of 3 judges", not a faked full panel |
| `fixture-3play` | **fixture** | 3 plays = panel-breach + single-oracle-breach + clean hold, on one screen | compact "whole verdict vocabulary" example |
| `midrun` | **fixture** | a run frozen **mid-attack** (a play still running) | shows the **telegraph** ghost + the in-progress play state |
| `degraded` | **fixture** | target blocked/errored most plays | honest broken-run handling (dashed "not assessed" + CRITICAL-untested) |
| *(`live`)* | **real, streaming** | the **live-bridge** mirror that fills in real-time (`?data=live&live=1`) | NOT in the `SAMPLES` list → the dropdown mis-shows the first option while on it (cosmetic, `OPEN-LV3`) |
