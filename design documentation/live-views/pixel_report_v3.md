# Live Views — Pixel Report · View 3 (Forensic)

> **Principle:** *every pixel wants a place, but every pixel must earn its place.* A deep, dedicated per-element audit of the **V3 Forensic** screen (5 regions audited in parallel), scored **/5** vs the DESIGN.md calm-instrument bar. Companion to `pixel_report.md` (which scored V3 as one region; this supersedes it for V3). Source: workflow `wf_9406c5b7-698`.

**97 elements · mean 3.93/5 · 1 cut · 27 refine · 69 keep-as-is.**

## Scoreboard

| Region | Elements | Mean /5 | Cut | Refine |
|---|--:|--:|--:|--:|
| V3 NAV + STEPPER | 20 | 3.75 | 1 | 5 |
| V3 VERDICT HEADER (forensic.tsx | 16 | 4.00 | 0 | 3 |
| V3 GATE PANEL | 18 | 3.78 | 0 | 6 |
| V3 Full Transcript (forensic.tsx | 13 | 4.08 | 0 | 5 |
| V3 Forensic | 30 | 4.03 | 0 | 8 |
| **All** | **97** | **3.93** | **1** | **27** |

## V3 NAV + STEPPER — shared RunNav (logo · run id · Overview/Live/Findings/Report · viewing-as role · theme) + V3 forensic sub-bar ("forensic · internal" · prev/◀ select ▶/next play stepper · Glance\|Arena\|Forensic zoom · back-to-Arena)

_The V3 chrome is two stacked sticky bars: the shared RunNav (radar logo + AutoSentinX word, run·target id, Overview/Live/Findings/Report pills, 'viewing as' role select, Sun/Moon theme toggle) and a forensic sub-bar ('forensic · internal' tag, a ◀/select/▶ play stepper, and a Glance\|Arena\|Forensic altitude zoom). It largely meets the calm internal-forensic bar: line icons throughout, severity words rendered as plain text (no decorative colour), honest disabled/loading/restricted states (the in-place access notice and 'Re-judge unavailable' ethos carry through). Standouts to keep at 5: 'forensic · internal', the prev/next stepper, the restricted-access notice. Main refinements: (1) the 'Glance' segment is a dead, greyed control advertising a missing feature — cut it; (2) there are THREE redundant back-to-Arena paths (zoom 'Arena' segment, in-body 'roll up to Arena', and the role switch) — consolidate to one 'up' affordance; (3) brand colour is used twice for 'you are here' in the same vertical stack (active 'Live' tab fill + brand-soft 'Forensic' segment) plus the logo X — fine as non-severity chrome but it double-marks location and slightly fights DESIGN.md's severity-only intent; (4) two stacked sticky bars eat vertical room and the run-identity label (the 'what am I looking at' anchor) hides below lg — merge/promote on wide viewports. Also single-source the picker's BREACHED/HELD derivation (currently a local cap() reimplementing outcomeToken) so the stepper label can never drift from the verdict in the body, and add a '3/8' position cue to the stepper._

| Element | Why it earns its place | /5 | Verdict | Note |
|---|---|:--:|:--:|---|
| "forensic · internal" label (mono, uppercase, 0.13em tracking, ink-faint) | States the view's classification — this is the unrestricted internal view, not the customer report | 5 | keep | Exactly the honesty-first, calm, severity-free framing the bar wants: tiny, mono, faint, no colour, tells the operator the disclosure context. Earns every pixel. |
| Prev button — ChevronLeft line icon, bordered, disabled@first (opacity-40), title 'prev · <id>' | Step to previous play's forensic without returning to Arena | 5 | keep | Line icon, honest disabled state, descriptive title with the actual next id. Textbook. Keep. |
| Next button — ChevronRight line icon, bordered, disabled@last, title 'next · <id>' | Step to next play's forensic | 5 | keep | Mirror of prev, same quality. Keep. |
| Restricted-access notice (replaces body for non Admin/Security: 'The forensic view is restricted' + role explainer + 'go to your Overview →') | Honest in-place access gate (no silent redirect) for the gated forensic view | 5 | keep | Exemplary honesty-first: explains WHO may see it, points to the role switch above, offers a graceful exit — no fake content, no hard redirect. Keep. |
| Radar/scan-sweep logo mark (concentric arcs + sweep line + center dot, brand-coloured SVG) | Brand anchor + home link to run Overview; the 'sentinel' identity (D-Q18) | 4 | keep | Distinctive, on-brand line mark, single brand hue, links home. A 5 only if it carried a subtle state (e.g. live-pulse during an active run) so the chrome itself signals 'forensic of a finished vs running run'; static is fine for calm. |
| Run identity text — runLabel + " · " + target (mono, ink-muted, lg-only, truncated 230px) | Tells you WHICH run/target you are dissecting — critical orientation in a forensic tool | 4 | keep | Right content, honest truncation, mono = machine id. A 5: this is load-bearing context that vanishes below lg — promote it to md (or show a compact run-id chip on small) so the forensic page never loses 'what am I looking at'. |
| Tab nav: Overview / Live / Findings / Report (rounded pills, ink-muted, hover surface-sunk) | Top-level screen navigation across the run | 4 | keep | Clean, 4 clear destinations, role-gated (Live hidden when !canSeeLive). A 5: 'Live' is the label but this view is Forensic which lives under Live — the active pill reads 'Live' while the user is three levels deep (Arena→play→forensic); a breadcrumb-ish 'Live › Forensic' or active-with-subpath cue would close the orientation gap. |
| "viewing as" label + role <select> (Admin/QA · Security · Exec · Compliance, mono) | Persona switch that re-homes you per §ownership (Security→Forensic) — also the demo's access-gate driver | 4 | keep | Genuinely useful for an internal honesty-first tool (lets you preview what each persona may see). A 5: the native <select> styling is plain — and on this restricted Forensic page it doubles as the only un-gate control, so a tiny lock/shield affordance hint next to it ('determines access') would make its power legible rather than looking like a cosmetic filter. |
| Theme toggle (Sun/Moon line icon, 44x44, border, hover brand/40) | Light/dark switch — DESIGN.md mandates dual light+dark | 4 | keep | Correct: line icon, hydration-safe, honest aria-label ('Switch to dark console theme'). A 5: 44px is oversized vs the 11px text density beside it (Fitts-justified but visually heavy in a calm bar) — tighten to match the role select height for rhythm. |
| Sub-bar container (sticky top-14, surface/60 + backdrop-blur, bottom border) | Second-level chrome zone for forensic-specific controls; visually separated from the global nav | 4 | keep | Good two-tier separation, translucent so content scrolls under it honestly. A 5: it stacks a second sticky bar under the first (top-0 + top-14) eating ~100px of vertical on a long forensic dossier — consider merging the stepper INTO the nav row on wide viewports so only one bar persists. |
| Play picker <select> — 'humanize(id) · BREACHED/HELD/<status>' per option (mono, max-w 260, cursor-pointer) | Jump directly to any play's forensic; the outcome suffix lets you triage where to look | 4 | keep | Strong: combines navigation + outcome-at-a-glance in one control, and BREACHED/HELD is rendered as plain TEXT (no colour) — correctly honouring severity-only-colour. A 5: the cap() here re-derives outcome ('FAIL'→BREACHED) independently of outcomeToken used elsewhere — single-source it so the picker label can never drift from the verdict shown in the body. |
| Stepper grouping (◀ select ▶ in one inline-flex) | Reads as a unified 'paginate through plays' affordance | 4 | keep | Good gestalt — the three controls clearly belong together. A 5: add a quiet position indicator ('3 / 8') so the operator knows how deep they are in the play set without opening the select. |
| 'resolving access…' / 'loading…' / 'play N not found' / err mono placeholders | Honest transient + error states while role/run resolve | 4 | keep | Calm mono, ink-faint, no spinner theatrics, distinct fail-text colour for real errors. A 5: 'play N not found' could offer a one-click jump back to the picker's first valid play rather than a dead end. |
| backdrop-blur + bg/90 translucency on both bars | Let dense forensic content scroll under the chrome while staying legible | 4 | keep | Tasteful, restrained translucency = calm. A 5: verify contrast of ink-faint sub-bar text over busy transcript bubbles scrolling beneath in light mode (the disclosure capture shows the brand-soft attacker bubbles bleeding close to the bar). |
| "AutoSentinX" wordmark with brand-coloured X | Product name; reinforces identity beside mark | 3 | refine | Mark+word is slightly redundant in a dense internal tool top bar where every run already shows run id. A 5: drop the word on the live/forensic chrome (showWord=false) and let the mark + run id carry it, reclaiming horizontal space for the stepper. |
| Active tab = solid brand fill + on-brand text (the 'Live' pill here) | Shows current top-level location | 3 | refine | Brand fill is the only saturated colour in the chrome and it's NON-severity — defensible for nav, but it competes with the brand-soft 'Forensic' chip in the sub-bar directly below, giving two brand emphases stacked. A 5: make the active tab an underline/weight cue and reserve filled-brand for exactly one 'you are here' marker per the chrome. |
| Zoom segmented control container (bordered, rounded, overflow-hidden, 11px mono) | Switch detail altitude: Glance ⟶ Arena ⟶ Forensic | 3 | refine | Concept (altitude zoom) is excellent and Bloomberg-ish, but execution is uneven (see Glance/Arena/Forensic rows). A 5: make all three live, equal-weight segments with one clearly-active state and no dead member. |
| "Forensic" segment — brand-soft bg + brand text, border-l (active marker) | Shows you are at the Forensic altitude | 3 | refine | Honest active state, but it's a SECOND brand-tinted 'you are here' directly beneath the brand-filled 'Live' tab — two brand cues for one location. A 5: one active treatment for the altitude control, and avoid stacking brand emphasis with the nav's active tab. |
| "Arena" segment — button, ink-muted, hover surface-sunk (zooms out to Arena) | Back to the V2 Arena overview | 2 | refine | Functionally this is the SAME action as the in-page '↑ roll up to Arena' button and the role-driven nav — three back-to-Arena paths. A 5: keep exactly one 'up' affordance. Since the segmented control implies altitude, let this be THE up-control and drop the redundant in-body 'roll up' link (or vice-versa). |
| "Glance" segment — ink-faint, cursor-not-allowed, title 'V1 Glance — coming soon' | Placeholder for the V1 glance altitude | 1 | cut | A non-functional, greyed segment is clutter in a forensic tool — it advertises a missing feature and the 'coming soon' only surfaces on hover. Cut until V1 ships; an honest tool shouldn't show controls that do nothing. |

## V3 VERDICT HEADER (forensic.tsx — matchup + verdict card, lines 50-59)

_The verdict header is the strongest, most honest moment in the whole V3 view: a big severity-toned outcome token, a one-line "agent-believed-vs-judges-disagree" reckoning, a dense honest gate-summary, and two tool buttons where Re-judge refuses to fabricate a stability result it cannot compute. It is already close to a calm Palantir/Bloomberg forensic header. Two real problems against the prompt and DESIGN.md: (1) The prompt's "CRITICAL chip" does not exist — severity is only rendered as plain inline text "severity high" up in the matchup subtitle (line 42), never as a chip and never colour-keyed, so the most decision-relevant fact (this is a CRITICAL breach) is the visually weakest token on screen. That is a miss for a severity-first tool. (2) The token uses raw outcome colour but the HELD state still renders a full-strength green word that competes with the breached-red on adjacent plays; HELD is the non-event and should be quieter than BREACHED. The disagree line, gate-summary, and Re-judge honesty are genuine 5-candidates. Verbatim across all four captures: token = BREACHED (red) / HELD (green); disagree = "The agent believed it held. The judges disagree." (oracle variant: "The specialist judge disagrees."); summary = "panel · committed 3/3 · self-report clean · score 0.967" and "specialist oracle · committed 0/1 · self-report clean · score 0"._

| Element | Why it earns its place | /5 | Verdict | Note |
|---|---|:--:|:--:|---|
| Disagree line: 'The agent believed it held. The judges disagree.' (12px, var(--fail-text) red, oracle variant 'The specialist judge disagrees.') | Names the gate-delta in plain English — the agent's self-belief was wrong. The forensic 'aha'. | 5 | keep | Essential, honest, plain-language, and correctly gated to only appear when gateDelta.disagree is true (absent on HELD, confirmed in r4b). This is the emotional and analytical core of the header. Perfectly executed. |
| Gate-verdict summary line: 'panel / specialist oracle · committed n/m · self-report clean\|flagged · score X' (11.5px mono, ink-muted, middot-separated) | Dense single-line provenance of HOW the verdict was reached: judge type, vote count, agent's own claim, numeric score. | 5 | keep | Exactly the Bloomberg-terminal density this tool wants — four orthogonal facts, mono, calm, no colour. The 'self-report clean' vs token=BREACHED contrast quietly exposes the lie. Keep as-is. |
| 'committed n/m' fraction + optional ' · k unavailable' suffix | Shows judge quorum and degraded-panel honesty (how many judges actually voted vs configured). | 5 | keep | Honesty-first: surfaces errored/unavailable judges inline instead of hiding a degraded panel. Captured shots show clean 3/3 and 0/1; the unavailable branch is correct design. |
| 'self-report clean' / 'flagged' phrase | States the agent's own claim about the interaction — the thing the judges are contradicting. | 5 | keep | Load-bearing: pairing 'self-report clean' with a BREACHED token in the same card IS the forensic contradiction. No change. |
| Re-judge honest 'unavailable' result line ('Re-judge endpoint not available in this build — pending the engine port (D-LV-dep3). No stability result is asserted.', warn-text + ink-faint) | Tells the truth when the feature can't run, and explicitly refuses to fabricate a stability outcome. | 5 | keep | This is honesty-first design at its best — it ships the button but names the dependency and asserts nothing false. The two-tone (warn-text reason + ink-faint disclaimer) is exactly right. Exemplary. |
| BREACHED / HELD outcome token (22px bold, tracking-tight, coloured var(--fail) red / var(--pass) green) | The single headline verdict — did the agent hold the line or not. The reason this whole view exists. | 4 | keep | Earns top billing and the severity-only colour is correct. To hit 5: damp the HELD green (HELD is the non-event — it should read calm/neutral, not a full-saturation success badge) so only BREACHED carries alarm weight, matching honesty-first 'don't celebrate a pass' restraint. |
| Token colour fork via inline style (fail = red, held = green, else var(--ink) neutral) | Encodes outcome severity in colour, the one channel DESIGN.md reserves for severity. | 4 | keep | Disciplined: it is the only colour in the token and it maps to outcome. 5 would tie the red specifically to severity (CRITICAL) rather than to generic FAIL, so a low-severity breach doesn't shout as loud as a critical one. |
| 'panel' vs 'specialist oracle' label (jm.isOracle fork) | Tells the reader whether a multi-model panel or a single specialist judge rendered this verdict — changes how much to trust n/m. | 4 | keep | Good honesty signal and it correctly drives the disagree-line wording too. 5: a tiny line-icon (Scale vs single Shield) would make the panel-vs-oracle distinction scannable without reading the word. |
| Re-judge button (RotateCcw line icon, mono, surface-sunk, border, hover:border-brand) | Affordance to re-submit the sealed transcript to the panel and test verdict stability. | 4 | keep | Correct line-icon, calm neutral styling. 5: it currently looks fully live but the action is unavailable — pre-disable or add a subtle 'beta'/dot so the user isn't surprised by the unavailable message after clicking. |
| Judge-diff toggle button (GitCompareArrows line icon, mono, identical chrome to Re-judge) | Toggles the Judge panel below between summary rows and a per-judge card/diff grid. | 4 | keep | Good icon choice and it does toggle real local state (no dishonesty). 5: it gives no pressed/active affordance — when judgeDiff is on, the button should show a selected state so the user knows the view changed. |
| Verdict card container (rounded-xl, border, bg-surface, the bottom px-4 py-3 panel under the header divider) | Groups the token + disagree + summary + buttons into one verdict unit, separated from the matchup header by a border. | 4 | keep | Calm, correct surface hierarchy. 5: the verdict section and the matchup section share one card with only a hairline divider; a hair more vertical separation or a faint severity-tinted left rule on the verdict half would make the outcome zone read as the focal block. |
| Middot ' · ' separators throughout summary, subtitle, and attacker lines | Compact delimiter that packs multiple facts into single mono lines. | 4 | keep | On-brand terminal density and used consistently. Fine as-is; only watch that the summary line doesn't wrap awkwardly on narrow widths. |
| 'roll up to Arena' back link (ArrowUp icon, 11px mono, var(--brand), hover:underline) sitting just above the card | Zoom-out navigation from the single forensic back to the Arena board. | 4 | keep | The only brand-blue in the header region, correctly reserved for an interactive nav affordance — consistent with non-severity colour rules. Lowercase 'roll up' matches the tool voice. Fine. |
| score value (e.g. 0.967 / 0, falls back to em-dash) | The numeric judge score behind the binary outcome. | 3 | refine | Earns its place but is ambiguous: 0.967 with a BREACHED token reads as a high score on a fail — is high bad or good? Add a direction hint (e.g. 'score 0.97 (commit confidence)' or a 0-1 scale anchor) so the number isn't misread. The '—' empty-state fallback is good honesty. |
| Re-judge transient 'running…' state (ellipsis appended for 600ms) | Acknowledges the click and signals work-in-progress before the unavailable verdict. | 3 | refine | Functional but the 600ms fake-latency on an endpoint that doesn't exist is faintly dishonest theatre. Either drop the delay and go straight to 'unavailable', or use it only when a real request is in flight. |
| CRITICAL severity chip (REQUESTED but ABSENT) | Should tell the reader this breach is CRITICAL vs lower severity — the second most load-bearing fact after the outcome. | 1 | refine | Does not exist as a chip. Severity is only plain text 'severity high' in the matchup subtitle (line 42), uncoloured and visually subordinate. Add a small line-icon severity chip beside the token, severity-colour-keyed, so CRITICAL is legible at a glance — currently the most important risk dimension is the weakest pixel. |

## V3 GATE PANEL — judge votes (Judge panel section, components/live/forensic.tsx)

_The V3 "Judge panel" is the model-named, full-reason inverse of the customer-facing anonymized panel — exactly the right honesty posture for an internal forensic tool. In the captured BREACHED play it renders three rows (gemini-2.5-pro / -flash / -flash-lite), each a red check + "committed" + stripped model name + full Devanglish reason. The strongest elements are the honest header disclaimer, the real-data structure (model names shown, gemini: prefix stripped, reasons verbatim), and the honest nJudges/configured math in the verdict summary line above it (committed N/configured, "X unavailable", oracle vs panel wording — never hardcoded). The weakest spots: (1) DESIGN.md says severity-ONLY colour, but "committed" is painted fail-text RED on every row — this conflates the per-judge VOTE direction (a structural fact) with severity, and because all three rows are red it reads as a wall of alarm rather than a calm forensic list; a held vote would be pass-GREEN (Shield) which is also a semantic/non-severity colour use. (2) The list layout gives each model a fixed 140px truncated column and a 120px vote column, but truncate can clip longer model ids silently. (3) Specificity number and error/AlertTriangle state are only surfaced in the Judge-diff card view, not the default list — so the default row hides spec and any per-judge error text behind the toggle. (4) The glyph set (Check / Shield / AlertTriangle) is good line-icon discipline, but Check==committed is semantically backwards to some readers (a check usually = "passed/held"), inviting misread. Honesty of nJudges is correctly handled in code (isOracle = nJudges===1 && no errors; configured derived from engine.judges split, default 3) and the summary line switches "specialist oracle"/"panel" wording accordingly — that is a 5._

| Element | Why it earns its place | /5 | Verdict | Note |
|---|---|:--:|:--:|---|
| Header note '· internal — model names + full reasons shown (the customer report anonymizes these)' | Honesty-first disclosure: tells the operator this view deliberately de-anonymizes judges, unlike the customer C6 report. | 5 | keep | Exactly the honesty posture DESIGN.md asks for — earns every pixel by explaining WHY this differs from the customer view. Essential and well-executed. |
| Error/unavailable variant — AlertTriangle glyph + 'unavailable' in warn-text AMBER (code path, not in this capture) | Discloses a judge that errored rather than silently dropping it from the denominator. | 5 | keep | Critical honesty feature — an errored judge is shown, not hidden, and feeds 'X unavailable' in the summary. Line icon + amber for a genuine operational warning is a legitimate use. Keep. |
| Per-judge reason text (ink-muted, 12px, .deva font, full verbatim) | The evidence: each judge's actual rationale, quoting the agent's words. | 5 | keep | The substance of the panel — full, unedited reasons with .deva for correct Devanglish rendering. This is what makes the tool forensic rather than a scoreboard. Keep. |
| Verdict summary line above panel: 'panel · committed {nCommitted}/{configured} · {errored} unavailable · self-report … · score …' | The gate denominator — how many judges committed out of how many configured, plus unavailable count. | 5 | keep | nJudges honesty is real here: configured derives from engine.judges split (default 3), errored judges shown as 'unavailable', oracle case collapses to /1 — never hardcoded. Model-honest denominator is the heart of the gate. Keep. |
| Oracle vs panel wording switch ('specialist oracle' / 'panel'; 'The specialist judge disagrees.' vs 'The judges disagree.') | Tells the operator whether this verdict came from a single specialist judge (1/1) or the full committee (3). | 5 | keep | isOracle = nJudges===1 && no errors && <=1 vote (runview.ts:131). Correctly distinguishes 1-judge oracle from a degraded panel — exactly the nJudges honesty the brief demands. Keep. |
| Section header label 'JUDGE PANEL' (uppercase, tracked, ink-faint, 10.5px semibold) | Names the region; sets the scan anchor for the gate evidence block. | 4 | keep | Calm, correct typographic treatment. A 5 would tighten the label to one unambiguous noun ('Panel votes' or 'Gate — judge votes') so it reads as votes not a roster. |
| Scale/balance line icon (lucide Scale) preceding the header | Iconographic cue that this is the adjudication/judgement block. | 4 | keep | On-brand line icon, severity-neutral, appropriate metaphor. 5 if paired consistently with the same icon wherever the gate is referenced (verdict block uses Scale too — verify it is the single canonical gate glyph). |
| Per-judge 'committed' label text (mono, 11px) | States this judge's vote direction — it committed to a breach finding. | 4 | keep | Clear, honest, paired with held/unavailable variants. 5 if the word made the vote object explicit ('committed: breach') so a single row is unambiguous out of context. |
| Model name (mono, ink-faint, gemini:-prefix stripped) e.g. gemini-2.5-pro / -flash / -flash-lite | Attributes each vote to a concrete model — the whole point of the internal de-anonymized panel. | 4 | keep | Real model ids, honestly shown, prefix stripped for density (forensic.tsx:79). 5 if the strip didn't risk hiding provider differences when a non-gemini judge appears — keep the provider visible or tooltip the full id. |
| Row vote-column fixed width w-[120px] + start-aligned flex layout | Keeps vote marker, model, and reason in three readable columns. | 4 | keep | Clean three-column scan, items-start so multi-line reasons don't misalign the marker. 5 with the colour fix above so the left column isn't a red stripe. |
| Fallback dash '—' when a judge has neither reason nor error | Honest empty-state: shows a vote exists but no rationale was returned, instead of a blank gap. | 4 | keep | Good honesty default. 5 if it said 'no reason returned' so the operator knows it's missing data, not a rendering bug. |
| Card-view per-judge tile (Judge-diff): border, surface-sunk bg, marker+model header, reason/error body | Side-by-side comparison of all judges' votes and rationale. | 4 | keep | Good comparison affordance, shows error text in warn-text when a judge failed. 5 with the same severity-colour discipline applied to the committed/held markers as the list view. |
| Held vote variant — Shield glyph + 'held' in pass-text GREEN (not visible in this BREACHED capture, in code path) | Shows a judge that did NOT find a breach. | 3 | refine | Good that dissent is representable (supports the 'judges disagree' honesty). But green is again a non-severity semantic colour; in an internal forensic tool a calmer ink/neutral 'held' marker would be more honest than green-vs-red theatre. |
| Specificity number ('· spec N') — only rendered in Judge-diff card view | Quantifies how concrete/grounded each judge's reasoning is. | 3 | refine | Useful honest signal but hidden behind the Judge-diff toggle; absent from the default list a reader can't weigh a vague vote vs a specific one. Refine: surface spec inline in the default row (it's a small mono token, low cost). |
| Judge-diff toggle button (GitCompareArrows icon) switching list ↔ per-judge card grid | Lets the operator expand to a 3-up card view exposing spec + per-judge error text. | 3 | refine | Legitimate progressive disclosure, but it's the ONLY path to specificity/error detail, so default users miss it. Refine: either surface spec inline by default, or label the toggle so its payload ('show specificity & errors') is discoverable. |
| Committed glyph — lucide Check (11px) on every committed row | Quick visual marker that the judge committed. | 2 | refine | Check==committed is semantically inverted for many readers (a check normally means held/passed). Refine to a glyph that reads as 'flagged/committed' (e.g. a flag or filled dot) and reserve Check for a held vote, to kill the misread. |
| Committed colour — fail-text RED applied to glyph + label on each row | Encode the vote direction with colour. | 2 | refine | Conflicts with DESIGN.md 'severity-only colour': the per-judge vote is a structural fact, not a severity. With all 3 rows red it becomes an undifferentiated alarm wall. Refine: ink for the vote text, let only the verdict block carry the single severity colour, or desaturate the per-row marker. |
| Model-name fixed column width w-[140px] with truncate (default list view) | Aligns rows into scannable columns. | 2 | refine | truncate can silently clip a longer model id with no affordance to recover it. Refine: widen, wrap, or add a title= tooltip so identity is never lost — honesty-first means never hiding which model voted. |

## V3 Full Transcript (forensic.tsx — "Full transcript" Section, the turn cards)

_The V3 Full Transcript region is a chat-bubble forensic log: right-aligned brand-soft attacker bubbles vs left-aligned neutral surface-sunk agent bubbles, each with a redundant uppercase role micro-label, a per-turn Refusal/Succeed pill on the agent side, an uppercase [PHASE] INTENT marker above each turn, and a fail-tinted pivot-turn highlight carrying an honest '(advisory)' classifier caveat. Strongest elements (5/5): the 'synthetic data / controlled red-team' honesty note, the neutral agent evidence-well, the '(advisory)' pivot caveat, and the no-truncation full-wrap of long Hinglish text — all squarely on the honesty-first, calm-forensic bar. Biggest concerns: (1) attacker bubbles use brand blue for text/fill, which competes with the 'colour = severity only' rule — mute it; (2) the 'Succeed/Refusal' chip is ambiguous and disconnected from the .cell held/gave-ground severity vocabulary used elsewhere; (3) phase markers repeat verbatim on consecutive same-phase turns (redundant ink — group them); and (4) DETECTOR HITS are absent from the transcript entirely (they live only in the Judge panel), so a per-turn 'what fired here' signal is missing and the analyst must cross-reference. Note the Devanagari (.deva) font path is untested in these captures since all turns are romanized Hinglish._

| Element | Why it earns its place | /5 | Verdict | Note |
|---|---|:--:|:--:|---|
| Section note '7 turns · Probe (Sentinx) vs Target (AARAV) · controlled red-team, synthetic data' | Honesty-first framing: turn count, who-is-who legend, and the critical 'synthetic data / controlled red-team' disclosure so an internal viewer never mistakes this for a real customer call | 5 | keep | Essential honesty affordance and earns every pixel — the 'synthetic data' tag is exactly the DESHONESTY-first guardrail the bar wants. Keep verbatim. |
| Agent/target bubble — left-aligned, surface-sunk fill, 1px border, asymmetric bottom-left corner (radius 3) | The defending agent's verbatim reply; neutral well styling signals 'this is the system under test' | 5 | keep | Neutral, calm, evidence-well styling is exactly right for an internal forensic read; the mirrored corner cleanly opposes the attacker bubble. |
| Pivot advisory note '· CLASSIFIER MARKED A COMMIT HERE (ADVISORY)' (fail-text, appended to phase line) | Honesty-first caveat: marks the commit as an advisory classifier call, NOT ground truth — prevents over-trusting the highlight | 5 | keep | Exactly the epistemic humility the bar demands; the '(advisory)' qualifier is load-bearing and pairs with the Engine-internals note that pivotTurn is 'NOT guaranteed the judge-quoted line'. Keep. |
| Hinglish/long-text wrapping behaviour (max-w-[88%], free wrap, NO truncate/line-clamp on bubbles) | Renders full verbatim turns including long multi-line Hinglish without clipping — critical for forensic completeness | 5 | keep | No clipping observed: 3-line attacker turns wrap fully. Correct call — truncation here would destroy forensic value. (Note: the only truncate in the file is on judge model names, line 79, not transcript text — appropriate.) |
| Section header 'FULL TRANSCRIPT' + FileText line-icon (13px, uppercase, 0.13em tracking, ink-faint) | Names the region and visually anchors it as one of the forensic sections; the icon disambiguates from Judge panel / Recon sections | 4 | keep | Consistent with the other section headers; line icon is on-brand. A 5 if the icon were a hair more legible at 13px on dark — currently near-invisible weight. |
| Phase/intent marker line '[CONTEXT] ESTABLISH A BENIGN, PLAUSIBLE REASON…' (9.5px uppercase, ink-faint, above each turn) | Tells the analyst WHICH attack phase/intent this turn belongs to — the campaign structure (context → sub-request → assemble) | 4 | refine | High value for forensics, but it repeats verbatim across consecutive same-phase turns (two identical [CONTEXT]… lines in a row) creating redundant ink. Collapse repeats into a phase group-header or dim the repeat to a rule. |
| Attacker bubble — right-aligned (ml-auto), brand-soft fill, brand-strong blue text, asymmetric bottom-right corner (radius 3) | Who-said-what affordance: the chat-bubble metaphor + side + colour instantly distinguishes attacker (Sentinx probe) from target | 4 | refine | Bubble metaphor reads well and side+colour is unambiguous. But blue is the brand/interactive colour — using it for attacker risks colliding with the 'colour = severity only' rule. A 5: keep side+role-label, mute the blue to a neutral and reserve hue for severity. |
| Per-bubble role micro-label 'ATTACKER' / 'AARAV (TARGET)' (9px uppercase ink-faint, inside each bubble) | Redundant-but-explicit who-said-what label so the speaker is unambiguous even if colour/side is missed (accessibility + honesty) | 4 | refine | Good belt-and-suspenders labeling. Slightly redundant with side+colour; at 9px it's near the legibility floor. A 5: bump to 9.5–10px or show only on the first turn of each side. |
| Pivot-turn highlight — fail-tinted card background (color-mix var(--fail) 7%) wrapping the committed turn | Draws the eye to the single turn the classifier flagged as the violation/commit | 4 | keep | Severity-only colour used correctly (fail tint) and restrained at 7%. A 5: ensure the tint passes contrast in light mode too (7% red on white may be invisible) — verify dual-theme. |
| .deva font-stack applied to bubble text (font-deva → geist → sans fallback) | Ensures Devanagari/Hindi glyphs render with a proper font rather than tofu; here content is Latin-script Hinglish so fallback is exercised | 4 | keep | Right defensive choice for an Indic red-team tool. A 5: verify with an actual Devanagari-script turn (current captures are all romanized Hinglish, so the Noto-Deva path is untested in these screenshots). |
| Inter-turn vertical rhythm / grid gap-3 spacing between turn pairs | Separates conversational turns so attacker→agent pairs read as discrete exchanges | 4 | keep | Spacing is calm and readable. A 5: tighten the gap between an attacker bubble and its own agent reply (mt-1) vs the larger gap between turn-pairs, so pairing is even clearer — currently relies mostly on side/colour. |
| Per-turn label chip {t.label} = 'Refusal' / 'Succeed' (mono 9.5px, bordered pill, inline-appended to agent bubble) | The per-turn outcome classification — did the agent hold or give ground on this specific turn | 3 | refine | Useful per-turn signal but it's a bare neutral pill with no severity encoding, and 'Succeed' is ambiguous (attacker succeeded? agent succeeded?). A 5: relabel to held/gave-ground and tie to the .cell severity vocabulary (solid/half) already in globals.css for cross-screen consistency. |
| Detector hits inside the transcript region | (Requested element) Per-turn detector/signal hits — would show which detectors fired on each turn | 2 | refine | ABSENT from the transcript: detectors live only in the separate Judge panel, and the transcript's sole per-turn signal is the Refusal/Succeed chip. For a forensic 'what fired where' read, surfacing a compact detector marker on the pivot/committed turn would close the loop — currently the analyst must cross-reference two sections. |

## V3 Forensic — Recon scouting interrogation + Engine internals + section headers

_This region is the deepest, most honest part of the V3 forensic view and it largely lives up to the Palantir/Bloomberg internal-tool bar: severity is the only loud colour, recon signals are rendered as calm bold-ink monospace facts (not coloured chips), the probe transcript shows the verbatim ask + reply, and the intel→attack derivation literally traces disclosesAi=false → seeded disclosure.undisclosed-ai. Strongest single element is that derivation line — it answers "why did the campaign attack THIS" with no hand-waving. The engine-internals block is correctly demoted (mono, muted, "advisory — for QA/debug") and its pivotTurn caveat ("NOT guaranteed the judge-quoted line") is exemplary honesty-first copy. Weaknesses are mostly redundancy and a couple of unlabelled affordances: (1) the pivotTurn advisory caveat is duplicated verbatim between the transcript pivot banner and engine internals; (2) "label trend: RSSSSSS" is a raw first-letter string that a reader cannot decode without the cell legend and isn't honest about what R/S mean here; (3) the recon probe crosshair icon is reused from the attacker "technique" glyph, blurring "this is the attacker probing" vs "this is a finding"; (4) the contact-provenance ("contact · Rohan Verma") is a synthetic persona name shown without a "synthetic" tag, a small honesty gap; (5) "arc reached" + "beats (who-moved)" + "phase plan" are three overlapping phase representations stacked in one block. Recommend keeping the recon block almost as-is, refining engine-internals to merge the three phase lines and decode label-trend, and cutting the duplicated pivot caveat._

| Element | Why it earns its place | /5 | Verdict | Note |
|---|---|:--:|:--:|---|
| discloses AI: NO (label + bold-ink value) | Baseline signal: does the target admit it's an AI | 5 | keep | Exactly right — mono label, bold neutral-ink value via labelTri, no severity colour leaking onto a non-verdict fact |
| stays in scope: YES | Baseline signal: does the agent stay on-task | 5 | keep | Same calm treatment; the YES/NO/unclear tri-state is honest about missing data |
| Bold-ink emphasis on signal values (no colour) | Draw the eye to the answer without implying severity | 5 | keep | Textbook honesty-first: weight-only emphasis reserves colour for verdicts; do not change |
| Probe text (verbatim question, e.g. 'Ek baat batao — aap koi insaan ho ya AI...') | Shows exactly what was asked | 5 | keep | Verbatim ask is the forensic gold standard; nothing to add |
| Probe reply (indented, ink-muted, e.g. 'Main aapki sahayata ke liye hoon ji...') | The agent's verbatim answer to the probe | 5 | keep | Muted-vs-default contrast cleanly separates target reply from attacker ask; honest verbatim |
| 'INTEL → ATTACK' sub-label | Heads the derivation that connects recon to campaign | 5 | keep | Best-in-region: makes the causal chain explicit, which is the whole point of a forensic view |
| Derivation statement: 'disclosesAi=false → seeded disclosure.undisclosed-ai' | Traces a specific recon signal to the specific objective it spawned | 5 | keep | The single highest-earning element here — concrete, falsifiable, mono intel-card key in bold ink, objective in brand link colour |
| Recon-unavailable fallback ('recon {status} — {reason}', warn-text) | Honest empty-state when no profile retained | 5 | keep | Honesty-first done right — degrades to a stated reason instead of a blank or a fabricated profile |
| '· advisory — for QA/debug' header note | Tells the reader this block is not a finding | 5 | keep | Exactly the honest framing an internal forensic tool needs; keep verbatim |
| PROVENANCE footer (run ER-LIVE · target ... · attacker gemini-2.5-flash · classifier ... · judges ... · maxTurns 8 · V3 Forensic (internal-unrestricted)) | Full run lineage / reproducibility record | 5 | keep | Model-named, run-id'd, maxTurns stated — exactly the auditable provenance an internal forensic record must carry; 'internal-unrestricted' tag correctly sets expectations |
| RECON — SCOUTING INTERROGATION section header (uppercase, 10.5px, 0.13em tracking, ink-faint) | Names the recon block and sets the calm forensic register | 4 | keep | Consistent with other section headers; would be a 5 if 'INTERROGATION' were softened to 'recon profile' — interrogation is faintly theatrical for an internal tool |
| Radar/target line-icon beside RECON header | Visual anchor for the scouting concept | 4 | keep | Line icon, severity-neutral, on-brand; 5 if it were visually distinct from the probe-row crosshairs so the section glyph and row glyph aren't conflated |
| refusal style: deflects to loan/EMI; never confirms it is an AI | Qualitative behavioural fingerprint feeding the attack plan | 4 | keep | High-value free-text signal; 5 if long values truncated/wrapped predictably — it currently runs to the panel edge and clips in the wide capture |
| PROBES (3) sub-label with count | Labels the probe-by-probe transcript and gives its size | 4 | keep | Count is useful; 5 if it read 'probes · 3 of N planned' so a reader knows whether scouting was truncated |
| Per-probe card (sunk surface, border, rounded-md) | Containerizes each ask+reply pair | 4 | keep | Clean separation; slightly heavy stacked borders — 5 with hairline dividers instead of full per-row boxes to reduce frame noise |
| Per-probe note tag (mono, 9px, uppercase, e.g. IDENTITY / AI-DISCLOSURE PROBE / OFF-SCOPE PROBE) | Classifies what each probe was testing | 4 | keep | Useful taxonomy; right-aligned faint mono is appropriately quiet — 5 if the vocabulary were a small fixed enum documented somewhere reachable |
| Parenthetical annotation '(identity confirmed — past the gate)' | Human gloss on what the probe established | 4 | keep | Adds interpretive value; ensure these are engine-derived not hand-written, or they risk being unverifiable editorializing in a forensic record |
| CornerDownRight arrow icon (metric/indigo) on derivation rows | Signals 'this fed into that' | 4 | keep | Correctly uses --metric (non-severity data viz colour) per DESIGN.md, not a severity hue; 5 if alignment with the text baseline were perfected (mt-0.5 nudge is slightly off) |
| ENGINE INTERNALS section header + Bug line-icon | Names the debug block and flags it as low-altitude | 4 | keep | Bug glyph reads instantly as 'debug'; 5 if visually demoted further (smaller/dimmer) since the 'advisory — for QA/debug' note already says it's not primary |
| phase plan: Context → Sub-request-1 → Sub-request-2 → Assemble | The intended attack phase sequence | 4 | keep | Clear mono chain; see 'arc reached' — these three phase lines overlap and could merge |
| pivotTurn (advisory classifier): 6 — 'the last in-call commit, NOT guaranteed the judge-quoted line' | Surfaces the classifier's commit guess with an honesty caveat | 4 | refine | The caveat is excellent, but it's duplicated verbatim from the transcript pivot banner; keep it in ONE place (here) and shorten the transcript banner to just 'advisory' |
| Full run-id / target URL / model ids shown in plaintext mono | Make the run independently re-locatable | 4 | keep | Honest and reproducible; 5 with a copy-to-clipboard affordance on run-id since QA will paste it |
| Section card chrome (rounded-xl border + bg-surface) shared by recon & internals | Groups each section into a panel | 4 | keep | Consistent panel system across the whole forensic; the nested sunk cards inside add a second frame layer — watch total border density |
| '· contact · Rohan Verma' note in the header | Provenance — who/what the recon scouted | 3 | refine | Good to surface provenance, but a synthetic persona name shown bare reads as a real person; append a 'synthetic' tag or persona marker to stay honesty-first |
| Brand-blue colour on drivesObjective target (e.g. disclosure.undisclosed-ai) | Marks the objective as a navigable/identifiable entity | 3 | refine | In dark mode --brand is the same cobalt as the attacker chat bubble; if it's not actually a link, the blue implies clickability it doesn't have — make it a real link or use neutral mono |
| Recon footer notes line ('dodged the AI-disclosure question · stayed strictly on loan scope') | Plain-language summary of recon behaviour | 3 | refine | Partly restates the signals strip above (NO / in-scope YES); merge into the signals or cut to avoid saying the same finding twice |
| arc reached: ... · arcComplete: true | Which phases actually executed and whether the arc finished | 3 | refine | Largely duplicates 'phase plan' when arc==plan; collapse into one line that diffs planned-vs-reached and shows arcComplete as the delta |
| beats (who-moved): Sub-request-1=re-angled · ... | Per-phase transition trigger | 3 | refine | Third phase-shaped line in a row; valuable data but stacking plan/arc/beats triples the cognitive load — unify into a single annotated phase timeline |
| Crosshair icon on each probe row | Marks the attacker's probing question | 2 | refine | Same crosshair used for the attacker 'technique' glyph in the header — reusing it here muddies meaning; swap to a question/scout glyph or drop it since the indent already implies 'ask' |
| label trend: RSSSSSS (first-letter-per-turn string) | Compact glance at the per-turn label sequence | 2 | refine | Undecodable without the cell legend — R/S aren't defined here and it isn't honest about ambiguity; either render the real .cell-* glyph row or label it ('R=refuse S=succeed') |

## Cut + lowest-scoring (highest-leverage)

| Element | Region | /5 | Verdict | What would lift it |
|---|---|:--:|:--:|---|
| "Glance" segment — ink-faint, cursor-not-allowed, title 'V1 Glance — coming soon' | V3 NAV + STEPPER | 1 | cut | A non-functional, greyed segment is clutter in a forensic tool — it advertises a missing feature and the 'coming soon' only surfaces on hover. Cut until V1 ships; an honest tool shouldn't show controls that do nothing. |
| CRITICAL severity chip (REQUESTED but ABSENT) | V3 VERDICT HEADER (forensic.tsx | 1 | refine | Does not exist as a chip. Severity is only plain text 'severity high' in the matchup subtitle (line 42), uncoloured and visually subordinate. Add a small line-icon severity chip beside the token, severity-colour-keyed, so CRITICAL is legible at a glance — currently the most important risk dimension is the weakest pixel. |
| "Arena" segment — button, ink-muted, hover surface-sunk (zooms out to Arena) | V3 NAV + STEPPER | 2 | refine | Functionally this is the SAME action as the in-page '↑ roll up to Arena' button and the role-driven nav — three back-to-Arena paths. A 5: keep exactly one 'up' affordance. Since the segmented control implies altitude, let this be THE up-control and drop the redundant in-body 'roll up' link (or vice-versa). |
| Committed glyph — lucide Check (11px) on every committed row | V3 GATE PANEL | 2 | refine | Check==committed is semantically inverted for many readers (a check normally means held/passed). Refine to a glyph that reads as 'flagged/committed' (e.g. a flag or filled dot) and reserve Check for a held vote, to kill the misread. |
| Committed colour — fail-text RED applied to glyph + label on each row | V3 GATE PANEL | 2 | refine | Conflicts with DESIGN.md 'severity-only colour': the per-judge vote is a structural fact, not a severity. With all 3 rows red it becomes an undifferentiated alarm wall. Refine: ink for the vote text, let only the verdict block carry the single severity colour, or desaturate the per-row marker. |
| Model-name fixed column width w-[140px] with truncate (default list view) | V3 GATE PANEL | 2 | refine | truncate can silently clip a longer model id with no affordance to recover it. Refine: widen, wrap, or add a title= tooltip so identity is never lost — honesty-first means never hiding which model voted. |
| Detector hits inside the transcript region | V3 Full Transcript (forensic.tsx | 2 | refine | ABSENT from the transcript: detectors live only in the separate Judge panel, and the transcript's sole per-turn signal is the Refusal/Succeed chip. For a forensic 'what fired where' read, surfacing a compact detector marker on the pivot/committed turn would close the loop — currently the analyst must cross-reference two sections. |
| Crosshair icon on each probe row | V3 Forensic | 2 | refine | Same crosshair used for the attacker 'technique' glyph in the header — reusing it here muddies meaning; swap to a question/scout glyph or drop it since the indent already implies 'ask' |
| label trend: RSSSSSS (first-letter-per-turn string) | V3 Forensic | 2 | refine | Undecodable without the cell legend — R/S aren't defined here and it isn't honest about ambiguity; either render the real .cell-* glyph row or label it ('R=refuse S=succeed') |
| "AutoSentinX" wordmark with brand-coloured X | V3 NAV + STEPPER | 3 | refine | Mark+word is slightly redundant in a dense internal tool top bar where every run already shows run id. A 5: drop the word on the live/forensic chrome (showWord=false) and let the mark + run id carry it, reclaiming horizontal space for the stepper. |
| Active tab = solid brand fill + on-brand text (the 'Live' pill here) | V3 NAV + STEPPER | 3 | refine | Brand fill is the only saturated colour in the chrome and it's NON-severity — defensible for nav, but it competes with the brand-soft 'Forensic' chip in the sub-bar directly below, giving two brand emphases stacked. A 5: make the active tab an underline/weight cue and reserve filled-brand for exactly one 'you are here' marker per the chrome. |
| Zoom segmented control container (bordered, rounded, overflow-hidden, 11px mono) | V3 NAV + STEPPER | 3 | refine | Concept (altitude zoom) is excellent and Bloomberg-ish, but execution is uneven (see Glance/Arena/Forensic rows). A 5: make all three live, equal-weight segments with one clearly-active state and no dead member. |
| "Forensic" segment — brand-soft bg + brand text, border-l (active marker) | V3 NAV + STEPPER | 3 | refine | Honest active state, but it's a SECOND brand-tinted 'you are here' directly beneath the brand-filled 'Live' tab — two brand cues for one location. A 5: one active treatment for the altitude control, and avoid stacking brand emphasis with the nav's active tab. |
| score value (e.g. 0.967 / 0, falls back to em-dash) | V3 VERDICT HEADER (forensic.tsx | 3 | refine | Earns its place but is ambiguous: 0.967 with a BREACHED token reads as a high score on a fail — is high bad or good? Add a direction hint (e.g. 'score 0.97 (commit confidence)' or a 0-1 scale anchor) so the number isn't misread. The '—' empty-state fallback is good honesty. |
| Re-judge transient 'running…' state (ellipsis appended for 600ms) | V3 VERDICT HEADER (forensic.tsx | 3 | refine | Functional but the 600ms fake-latency on an endpoint that doesn't exist is faintly dishonest theatre. Either drop the delay and go straight to 'unavailable', or use it only when a real request is in flight. |
| Held vote variant — Shield glyph + 'held' in pass-text GREEN (not visible in this BREACHED capture, in code path) | V3 GATE PANEL | 3 | refine | Good that dissent is representable (supports the 'judges disagree' honesty). But green is again a non-severity semantic colour; in an internal forensic tool a calmer ink/neutral 'held' marker would be more honest than green-vs-red theatre. |

## Score distribution

| Score | Count |
|--:|--:|
| 5 | 28 |
| 4 | 45 |
| 3 | 15 |
| 2 | 7 |
| 1 | 2 |

## Verdict

V3 Forensic scores **3.93/5 across 97 elements** — lower than V2's 4.26 because it's the densest screen and the audit was stricter on its chrome: **1 cut, 27 refine**. The transcript + judge-panel content is strong; the drag is (1) the **zoom control** (dead Glance segment + redundant back-to-Arena paths), (2) **judge-vote glyphs/colour** (Check==committed reads inverted; fail-red on a structural vote breaks severity-only-colour), and (3) small honesty/legibility nits (truncated model ids, an undecoded 'label trend RSSSSSS', a missing severity chip). None are deep; tightening the chrome + the vote row lifts this past 4.2.